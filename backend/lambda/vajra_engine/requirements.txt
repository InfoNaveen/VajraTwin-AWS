# ---------------------------------------------------------------------------
# VajraTwinEngine Lambda – runtime dependencies
# ---------------------------------------------------------------------------
# boto3 / botocore are pre-installed in the Lambda Python 3.12 runtime, but
# pinning them here ensures `sam build` packages a consistent, tested version
# and that local unit tests use the same library.
# ---------------------------------------------------------------------------
boto3==1.34.144
botocore==1.34.144
